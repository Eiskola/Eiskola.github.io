---
title: 线性空间——线性空间的基与维数
published: 2025-10-22
description: 本文介绍线性空间的基与维数相关概念与性质
image: ../images/cover.png
tags: [矩阵分析, 基, 维数]
category: 高等工程数学
draft: false
---

# 1.1.3 线性空间的基与维数
## 基与维数
:::important
&emsp;&emsp;设 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ 是`线性空间` $V$ 中的`线性无关`向量组，若任意 $\beta \in V$ 都可以被 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ 线性表出，则，

1. 称 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ 是 $V$ 的`基`；
2. 称 $V$ 的`维数`为 $n$，即 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ 中向量数量。记作 $dim(V) = n$；
3. 称 $V$ 是一个 $n$ `维线性空间`，通常记作 $V^n$；
4. $dim(\textbf{0}) = 0$.
:::

:::tip
&emsp;&emsp;`线性空间` $V$ 的一组`基`就是该线性空间的一组`极大线性无关组`。上面的定义中实际上隐含了这个事实，$\forall \beta \in V$ 都可以被 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ 线性表出，也就是说向该向量组中加入任意一个向量就会变得线性相关。
:::

### 示例
&emsp;&emsp;下面给出一些具体例子。

:::note
1. $V = (\mathbb{R}^n, \mathbb{R}, +, \cdot)$

&emsp;&emsp;基：
$$
\left\{
    \begin{bmatrix}
        1 \\ 0 \\ \vdots \\ 0
    \end{bmatrix},
    \begin{bmatrix}
        0 \\ 1 \\ \vdots \\ 0
    \end{bmatrix}, ..., 
    \begin{bmatrix}
        0 \\ 0 \\ \vdots \\ 1
    \end{bmatrix}
\right\}
$$

&emsp;&emsp;维数：
$$
dim(V) = n
$$
:::



:::note
2. $(\mathbb{C}^n, \mathbb{C}, +, \cdot)$

&emsp;&emsp;基：
$$
\left\{
    \begin{bmatrix}
        1 \\ 0 \\ \vdots \\ 0
    \end{bmatrix},
    \begin{bmatrix}
        0 \\ 1 \\ \vdots \\ 0
    \end{bmatrix}, ..., 
    \begin{bmatrix}
        0 \\ 0 \\ \vdots \\ 1
    \end{bmatrix}
\right\}
$$

&emsp;&emsp;维数：
$$
dim(V) = n
$$

&emsp;&emsp;复数的虚数部分可以由 $k \in \mathbb{C}$ 引入.
:::


:::note
3. $(\mathbb{C}^n, \mathbb{R}, +, \cdot)$

&emsp;&emsp;基：
$$
\left\{
    \begin{bmatrix}
        1 \\ 0 \\ \vdots \\ 0
    \end{bmatrix},
    \begin{bmatrix}
        i \\ 0 \\ \vdots \\ 0
    \end{bmatrix},
    \begin{bmatrix}
        0 \\ 1 \\ \vdots \\ 0
    \end{bmatrix},
    \begin{bmatrix}
        0 \\ i \\ \vdots \\ 0
    \end{bmatrix}, ..., 
    \begin{bmatrix}
        0 \\ 0 \\ \vdots \\ 1
    \end{bmatrix}
    \begin{bmatrix}
        0 \\ 0 \\ \vdots \\ i
    \end{bmatrix}
\right\}
$$

&emsp;&emsp;复数的虚数部分不能从 $k \in \mathbb{R}$ 中引入，只能从基中引入.
:::

:::note
4. $(\mathbb{R}^{n \times n}, \mathbb{R}, +, \cdot)$

&emsp;&emsp;基：
$$
\left\{ 
\begin{bmatrix} 1 & 0 & \cdots & 0 \\ 0 & 0 & \cdots & 0 \\ \vdots & \vdots & \ddots & \vdots \\ 0 & 0 & \cdots & 0 \end{bmatrix}, 
\begin{bmatrix} 0 & 1 & \cdots & 0 \\ 0 & 0 & \cdots & 0 \\ \vdots & \vdots & \ddots & \vdots \\ 0 & 0 & \cdots & 0 \end{bmatrix}, 
\cdots, 
\begin{bmatrix} 0 & 0 & \cdots & 0 \\ 0 & 0 & \cdots & 0 \\ \vdots & \vdots & \ddots & \vdots \\ 0 & 0 & \cdots & 1 \end{bmatrix} 
\right\}
$$

&emsp;&emsp;维数：
$$
dim(V) = n \times n
$$
:::

:::note
5. $(P_n(t), \mathbb{R}, +, \cdot)$

    :::tip
    其中，$P_n(t)$ 表示次数不超过 $n$ 的实系数多项式.
    :::

&emsp;&emsp;基：
$$
\left\{ 
1, t, t^2, ..., t^n
\right\}
$$

&emsp;&emsp;维数：
$$
dim(V) = n+1
$$

&emsp;&emsp;注意`不要遗漏了常数项`.
:::

:::note
6. $(V_i, \mathbb{R}, +, \cdot)$，其中，
$$
\
V_1 = 
\left\{
    \begin{bmatrix}
        a & b \\
        c & 0
    \end{bmatrix}
    : a, b, c \in \mathbb{R}
\right\}
$$
$$
V_2 = 
\left\{
    \begin{bmatrix}
        a & b \\
        -b & c
    \end{bmatrix}
    : a, b, c \in \mathbb{R}
\right\}
$$

- 对于 $V_1$：
    &emsp;&emsp;基：
    $$
    \left\{ 
        \begin{bmatrix}
            1 & 0 \\
            0 & 0
        \end{bmatrix},
        \begin{bmatrix}
            0 & 1 \\
            0 & 0
        \end{bmatrix},
        \begin{bmatrix}
            0 & 0 \\
            1 & 0
        \end{bmatrix}
    \right\}
    $$

    &emsp;&emsp;维数：
    $$
    dim(V_1) = 3
    $$

- 对于 $V_2$：
    &emsp;&emsp;基：
    $$
    \left\{ 
        \begin{bmatrix}
            1 & 0 \\
            0 & 0
        \end{bmatrix},
        \begin{bmatrix}
            0 & 1 \\
            -1 & 0
        \end{bmatrix},
        \begin{bmatrix}
            0 & 0 \\
            0 & 1
        \end{bmatrix}
    \right\}
    $$

    &emsp;&emsp;维数：
    $$
    dim(V_2) = 3
    $$
:::

:::note
7. $\mathscr{N}(A)$ 和 $\mathscr{R}(A)$ 其中 $A \in F^{m \times n}$

    :::tip
    [$\mathscr{N}(A), \mathscr{R}(A)$ 定义](https://eiskola.github.io/posts/aem/1-%E7%9F%A9%E9%98%B5%E5%88%86%E6%9E%90/11-%E7%BA%BF%E6%80%A7%E7%A9%BA%E9%97%B4/111-%E7%BA%BF%E6%80%A7%E7%A9%BA%E9%97%B4%E7%9A%84%E6%A6%82%E5%BF%B5/#4-%E7%9F%A9%E9%98%B5%E7%9A%84%E9%9B%B6%E7%A9%BA%E9%97%B4%E4%B8%8E%E5%80%BC%E7%A9%BA%E9%97%B4)
    :::

- 对于 $\mathscr{N}(A) = \{x \in F^n | Ax=0\}$

&emsp;&emsp;根据`零空间` $\mathscr{N}(A)$ 定义，**$\mathscr{N}(A)$ 是矩阵方程 $Ax=0$ 的解空间**，而该解空间的任意解向量能够由 $\mathscr{N}(A)$ 的一组基`线性表示`。因此，线性空间 $\mathscr{N}(A)$ 的一组基实际上就是该解空间的一组`基础解系`。

&emsp;&emsp;而该基础解系中向量的个数就等于`零空间` $\mathscr{N}(A)$ 维数。对于基础解系而言，其向量个数等于矩阵方程中`自由变量`个数，于是有，
$$
dim(\mathscr{N}(A)) = n-rank(A) = n-r
$$
其中 $rank(A) = r$ 即为矩阵 $A$ 的`秩`。实际上这被称为`秩-零化度定理`：
$$
dim(\mathscr{N}(A)) + rank(A) = n
$$

&emsp;&emsp;那么如何求解矩阵方程 $Ax = 0$ 解空间 $\mathscr{N}(A)$ 的基础解系呢？

&emsp;&emsp;求解零空间或 $Ax=0$ 基础解系的一般步骤为： 1. 化矩阵 $A$ 为`行最简型`；2. 确定`主变量`与`自由变量`；3. 用`自由变量表示主变量`；4. 构造`基础解系`。


- 对于 $\mathscr{R}(A) = \{y=Ax | x \in F^n\}$

&emsp;&emsp;`值空间` $\mathscr{R}(A)$ 也表示矩阵 $A \in F^{m\times n}$ 的`列空间`，即**由矩阵 $A$ 的列向量张成的空间**。于是，矩阵 $A$ 列向量的`极大线性无关组`也是值空间 $\mathscr{R}(A)$ 一组基。而值空间 $\mathscr{R}(A)$ 的维数是矩阵 $A$ 列向量的极大线性无关组的向量个数，实际上就是矩阵 $A$ 的秩 $rank(A) = r$，因此，
$$
dim(\mathscr{R}(A)) = rank(A) = r
$$
:::

## 坐标
:::important
**定理**：设 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ 是 $n$ 维线性空间 $V$ 的一组基，则 $\forall \beta \in V$ 可以被 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ `唯一地`线性表示，也即：`存在唯一确定`的一组 $x_1, x_2, ..., x_n \in F$ ，使得
$$
\beta = x_1\alpha_1 + x_2\alpha_2 + ... + x_n\alpha_n
$$

&emsp;&emsp;$x = [x_1, x_2, ..., x_n]^T$ 称为 $\beta$ 在基 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ 下的`坐标向量`，简称`坐标`。

&emsp;&emsp;记 $A = [\alpha_1, \alpha_2, ..., \alpha_n]$，则 $\beta = Ax$
:::

:::important
&emsp;&emsp;任意 $n$ 维线性空间 $V^n$ `同构`于 $F^n$，记作 $V^n \cong F^n$。
:::

### 线性空间的同构
:::important
&emsp;&emsp;对数域 $F$ 上的两个线性空间 $V_1, V_2$，若存在`一一映射` $\sigma: V_1 \rightarrow V_2$，满足：对 $\forall \alpha, \beta \in V_1$ 和 $\forall k, l \in F$，总有
$$
\sigma(k\alpha + l\beta) = k\sigma(\alpha) + l\sigma(\beta)
$$
则称，$V_1, V_2$ `同构`，$\sigma$ 称为`同构映射`。

&emsp;&emsp;也即，`同构映射可以与线性运算交换次序`。
:::

### 同构的性质
:::important
1. $\sigma(0) = 0, \sigma(-\alpha) = -\sigma(\alpha)$.

    :::caution
    这里的 `0` 应该视为线性空间中的零元 $\mathbb{0}$.
    :::

&emsp;&emsp;证明如下：
$$
\begin{align*}
    & \sigma(\alpha) + \sigma(0) = \sigma(\alpha + 0) = \sigma(\alpha) \\
    & \Rightarrow -\sigma(\alpha) + \sigma(\alpha) + \sigma(0) = -\sigma(\alpha) + \sigma(\alpha) \\
    & \Rightarrow 0 + \sigma(0) = 0 \\
    & \Rightarrow \sigma(0) = 0
\end{align*}
$$

2. $\sigma(\sum_{i = 1}^r k_i\alpha_i) = \sum_{i = 1}^r \sigma(\alpha_i), k_i \in F, i = 1, 2, ...,r.$

&emsp;&emsp;根据同构定义不难证明：
$$
\begin{align*}
    & \sigma(\sum_{i = 1}^r k_i\alpha_i) = \sigma(\sum_{i = 1}^{r-1} k_i\alpha_i) + \sigma(k_r\alpha_r) \\
    & = \sigma(\sum_{i = 1}^{r-2} k_i\alpha_i) + \sigma(k_{r-1}\alpha_{r-1}) + k_r \sigma(\alpha_r) \\
    & \dots \\
    & = \sigma(k_1\alpha_1) + k_2\sigma(\alpha_2) + ... + k_r\sigma(\alpha_r) \\
    & = k_1\sigma(\alpha_1) + k_2\sigma(\alpha_2) + ... + k_r\sigma(\alpha_r) \\
    & = \sum_{i = 1}^r k_i\alpha_i
\end{align*}
$$


3. $V_1$ 中的向量组 $\{\alpha_1, \alpha_2, ..., \alpha_n\}$ 线性相关，当且仅当 $\{\sigma(\alpha_1), \sigma(\alpha_2), ..., \sigma(\alpha_r)\}$ 线性相关.

4. 同构的线性空间`维数相同`，`基一一对应`.
:::

### 给定基求向量的坐标
:::important
例：取 $P_2(t)$ 的基：
$$
A = \{t+1, t+2, t^2\}
$$

求

$$
p(t) = 2t^2 - t + 1
$$

在 $A$ 下的坐标.

解：设
$$
\begin{align*}
    x_1(t+1) + x_2(t+2) + x_3(t^2) = 2t^2-t+1 & \\
     = x_3t^2 + (x_1 + x_2)t + (x_1 + 2x_2)
\end{align*}
$$

也即
$$
\begin{bmatrix}
    0 & 0 & 1 \\
    1 & 1 & 0 \\
    1 & 2 & 0
\end{bmatrix}
\begin{bmatrix}
    x_1 \\
    x_2 \\
    x_3
\end{bmatrix} = 
\begin{bmatrix}
    2 \\
    -1 \\
    1
\end{bmatrix}
$$
记
$$
M = \begin{bmatrix}
    0 & 0 & 1 \\
    1 & 1 & 0 \\
    1 & 2 & 0
\end{bmatrix}, 
b = \begin{bmatrix}
    2 \\
    -1 \\
    1
\end{bmatrix}
$$
求解这个`矩阵方程` $Mx = b$，显然 $M$ **可逆**，`否则不存在唯一解`，即在基下的坐标。于是

$$
x = \begin{bmatrix}
    x_1 \\
    x_2 \\
    x_3
\end{bmatrix}
 = M^{-1}b
$$

可解
$$
\begin{bmatrix}
    x_1 \\
    x_2 \\
    x_3
\end{bmatrix} = 
\begin{bmatrix}
    -3 \\
    2 \\
    2
\end{bmatrix}
$$
:::